帮我生成一张横版学术海报，比例 16:9，高分辨率，干净专业的学术风格，主色调为深蓝色（#003366）和白色。

标题（顶部居中，大号粗体）："面向无人机安全自主飞行的 ODE 初值问题数值解法稳定域分析与安全边界形式化验证"

副标题（标题下方，小一号字体）："基于 Crazyflie 2.1 集群的 ODE 数值仿真、运行时安全监视与求解器对比实验框架"

海报主体分为 2×2 四块区域：

左上区块标题："障碍物避障子系统"。画面主体是一个呼吸圆环编队，5 架微型四旋翼无人机形成一个圆环阵列，围绕 5 根红色半透明圆柱障碍物飞行。圆柱位于中心位置和四个对角方位。无人机周围用红色箭头标注径向排斥力，用蓝色箭头标注切向绕行引导力。右下角有一个小插图，展示三次方平滑势场曲线（绿色 t^3）与经典 1/d^2 势场（红色）的力场对比图。底部标注："平滑三次方势场 (t^3) + 速度阻尼 + 切向引导 — 远场不干扰编队，近场不穿透障碍物"。

右上区块标题："运行时安全监视器 (SafetyMonitor)"。展示一个仪表盘风格的界面，包含 4 个圆形量表，分别标注为：碰撞安全 □(d_surf>0)、速度安全 □(|v|<2.5 m/s)、姿态安全 □(|θ|<0.8 rad)、能量漂移 □(ΔE<0.01 J)。每个量表有绿色/黄色/红色三区刻度。量表下方有一个 CSV 表格示例，展示违规记录的行格式（时间、无人机ID、条件类型、实际值、阈值）。右上角有一个醒目的 "安全分数" 徽章，公式为 "Overall = min(碰撞合规率, 速度合规率, 姿态合规率)"。底部标注："每步形式化不变式检查 — 四维安全条件在线监控，违规日志与安全分数自动输出"。

左下区块标题："求解器稳定域对比实验框架"。上半部分展示一个实验矩阵，5 列（Euler/Heun/RK4/RK45/Implicit Euler）× 5 行（步长 0.001~0.05s）。下半部分展示 4 张小型对比图表：能量漂移率对比柱状图、障碍物最小距离对比图、RMS 位置误差对比图、单步计算耗时对比图。每张图用不同颜色区分 5 个求解器。中间有一个红色高亮标注框，写着核心发现："安全步长 < 理论稳定步长 — 数值稳定性是安全飞行的必要条件，但绝非充分条件"。底部小字："全自动 150 次因子实验 | 六维评估指标体系 | 临界步长二分搜索 | 批量 CSV 产出与 Python 可视化流水线"。

右下区块标题："区间可达集近似分析 (Interval Reachability)"。展示一个 3D 视角的场景：一架小无人机周围有一个淡青色半透明的 AABB 包围盒（标注 ±ε 状态扰动），附近立着一根红色圆柱障碍物。用双向箭头示意对 12 维状态每维施加正负 ε 扰动。画面中展示两条公式：采样法公式 "25 次求解器调用 = 1 中心 + 12维 × 2方向"，Jacobian 法公式 "13 次求解器调用 = |J|·ε 线性传播"。下方对比两者的计算速度。底部标注："保守最坏情况碰撞预警 — 如果在 ±ε 扰动下可达集与障碍物无交集，则当前状态下绝对不可能碰撞"。

海报最底部是一条窄色带，展示技术支持信息："ROS 2 Humble | Eigen 3 | C++17 | yaml-cpp | Python3 + matplotlib | 5 种求解器：前向欧拉法 / 改进欧拉法 (Heun) / 经典 RK4 / RK45 自适应 / 隐式欧拉法 (A-稳定)"

整体风格：矢量扁平化设计，等距 3D 元素用于无人机集群，专业的学术海报美学，中文黑体或思源黑体，英文部分用 Helvetica。背景为极淡的浅灰蓝色（#F0F2F5），区块之间用细的深蓝色分隔线。所有图表和数据均为矢量风格插画，不需要真实照片。色彩强调：海军深蓝 (#003366)、青色高亮 (#00A8E8)、红色警示点 (#E63946)。
