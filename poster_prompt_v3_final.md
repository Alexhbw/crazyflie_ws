# 学术海报生成 Prompt — 16:9 宽屏海报

请生成一张 **16:9 宽屏学术海报**（横向，适合投影幕布/大屏幕展示），用于无人机数值解法与安全形式化验证的硕士论文答辩。海报风格要求：**现代学术风格、深蓝色主色调、信息层级清晰、图表化表达优先**。

---

## 整体布局（从上到下，五段式）

最上方：通栏深蓝色标题横幅
上半部中央：研究总览 + 技术路线图（横向流程图）
中部：左右两栏 — 左栏【五大求解器 + 安全监视器】、右栏【安全控制器 + 反例挖掘】
下半部：左右两栏 — 左栏【区间可达集 + Monte Carlo】、右栏【关键实验发现】
底部：通栏深蓝色结论横幅

---

## 一、标题横幅（顶部通栏，占高度 12%）

- 背景色：深蓝色 (#003366)
- 主标题（白色大字）：面向无人机安全自主飞行的 ODE 初值问题数值解法稳定域分析与安全边界形式化验证
- 副标题（浅灰小字，主标题下方）：—— 基于 Crazyflie 2.1 集群的数值仿真、在线安全控制与形式化验证平台
- 右侧角落标注：2026.06 | 硕士论文答辩

---

## 二、研究总览区（标题下方，占高度 18%）

该区域分为三个横向排列的模块：

**模块A — 研究动机（左侧 1/3）**：
- 图标 + 文字：Crazyflie 2.1 微型四旋翼（质量27g，转动惯量10^-5量级）
- 核心矛盾：[强闭环PID × 刚性问题] → 求解器选择 = 安全决策
- 三个关键词标签（带圆角色块）：数值刚性、安全临界步长、形式化验证

**模块B — 技术路线图（中间 1/3）**：
- 5个圆角矩形箭头串联（蓝色渐变）：
  理论建模(12维ODE) → 雅可比稳定域分析 → CORA可达性验证 → ROS2工程仿真 → 在线安全控制闭环
- 下方标注：全链路闭环验证平台

**模块C — 平台指标（右侧 1/3）**：
- 5种求解器 | 12维状态 | 4机集群 | 5障碍物 | 150次全因子实验
- 用大数字 + 图标呈现，视觉冲击力强

---

## 三、核心模块详解区（占高度 50%，左右两栏）

### 左栏（占宽度 52%）：

**A. 五大ODE求解器谱系（上方 40%）**
一张横向对比图/表格，展示：
| 求解器 | 阶数 | 单步求值 | 稳定性 | 安全步长 |
|--------|------|----------|--------|----------|
| 前向欧拉 Euler | O(h) | 1次 | 条件稳定 | ≤0.02s |
| 改进欧拉 Heun | O(h²) | 2次 | 条件稳定 | ≤0.02s |
| 经典RK4 | O(h⁴) | 4次 | 条件稳定 | ≤0.02s |
| RK45自适应 | O(h⁴)/O(h⁵) | 6+子步 | 自适应 | ≤0.8s ⭐ |
| 隐式欧拉 | O(h) | ~65次 | A-稳定 | ≤0.02s |

旁边放一张五条不同颜色的轨迹线对比示意图（表现Euler发散vs RK45稳定）

**B. 运行时安全监视器 SafetyMonitor（下方 30%）**
- 中心图标：一个盾牌，内部四个勾选框
- 四维□不变式（带图标）：
  □ 碰撞安全：d_surf > 0
  □ 速度安全：|v| < 2.5 m/s
  □ 姿态安全：|θ| < 0.8 rad
  □ 能量漂移：|ΔE| < ε
- 下方小字：安全分数 = min(各维度合规率) | 预热2.0s | CSV实时输出

**C. 区间可达集 IntervalReachability（下方 30%）**
- 左上：一个半透明蓝色包围盒（AABB） + 红色圆柱体（障碍物），表现区间包络与碰撞检测
- 标注：采样法 25次/步 | Jacobian法 13次/步
- 核心公式（简洁）：F(X₀) ⊆ [p_low, p_high]

### 右栏（占宽度 48%）：

**D. 在线安全控制器 SafetyController（上方 35%）**
- 三级递进示意图（纵向堆叠的3个色块，绿色→黄色→红色）：
  L1 绿色: smoothed < 0.7 → 减半步长 (REDUCE_DT)
  L2 橙色: smoothed < 0.3 → 切换更保守求解器 (SWITCH_SOLVER)
  L3 红色: smoothed < 0.05 → 紧急关机 (EMERGENCY_STOP)
- 左侧箭头标注：EMA指数平滑 (α=0.05) | 求解器安全层级 Euler→Heun→RK4→RK45→Implicit
- 底部小字验证结果：Euler dt=0.3s测试，1.5s成功触发L1

**E. 反例自动挖掘 Counterexample Mining（下方 35%）**
- 一张简化的安全热力图（绿红渐变色块矩阵），行=求解器(5行)，列=步长(0.01~2.0s)：
  突出显示：dt=0.05s这一列，Euler/Heun/RK4/Implicit全是红色方块(X)，只有RK45是绿色方块(S)
- 热力图右侧标注三个关键反例：
  dt=0.05s: Heun(0.07)❌ → RK45(1.00)✅
  dt=0.05s: RK4(0.05)❌ → RK45(1.00)✅
  dt=0.05s: Implicit(0.03)❌ → RK45(1.00)✅
- 底部论文级结论文字框（红底白字）：
  "求解器选择不是计算效率的取舍 — 而是生死攸关的安全决策"

**F. 多步可达管 + Monte Carlo（下方 30%）**
- 左侧：可达管示意图 — 一条名义轨迹线 + 围绕它的k步区间包络管（半透明蓝色）
- 右侧：两张小图并排 — 障碍物距离分布直方图 + 确定性区间vs蒙特卡洛对比柱状图
- 核心数据标注：200样本 | 100%覆盖率 | 过近似度~4×
- 结论标签（绿色）：确定性区间方法——保守但工程可用

---

## 四、关键发现与结论区（底部，占高度 15%）

**左半部分：三大核心发现（三个横向排列的圆角卡片）**：
卡片1（橙色边框）: 🔑 安全步长 < 理论稳定步长 — 安全边界比数值稳定边界窄3~5倍
卡片2（蓝色边框）: 🔑 RK45自适应法独树一帜 — 0.05~0.8s大步长下唯一安全飞行的求解器
卡片3（绿色边框）: 🔑 确定性区间100%覆盖MC样本 — 形式化安全保证，工程代价可接受

**右半部分：总结语（大字）**：
"将数值分析、形式化方法与在线控制工程实现有机融合，
 为无人机安全自主飞行提供从理论到实践的完整验证平台。"

---

## 五、底部信息条（最底部，占高度 5%）

深蓝色背景条，白色小字：
左侧：Crazyflie 2.1 × ROS 2 Humble × Eigen3 × C++/Python | 右侧：2026年6月 · 硕士论文答辩

---

## 全局设计规范

- **配色方案**: 主色深蓝 #003366，辅色浅蓝 #4A90D9，强调色橙色 #E8833A，绿色 #339933，红色 #CC3333
- **背景**: 纯白色 + 淡蓝灰 #F2F5F9 的浅色区域分隔
- **字体风格**: 标题用粗黑体，正文用无衬线体，所有文字必须清晰可读，中文优先
- **间距**: 各模块之间用细线或色带分隔，保持呼吸感
- **图表风格**: 扁平化、简洁、无多余装饰，数据标注清晰
- **整体观感**: 专业学术会议海报风格，信息密度高但不凌乱，视觉层次分明
